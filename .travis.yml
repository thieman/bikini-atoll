language: objective-c

install:
  - brew update
  - brew install nginx
  - brew tap caskroom/cask
  - brew cask install virtualbox
  - brew install boot2docker
  - boot2docker init
  - boot2docker up

script:
  - ls
